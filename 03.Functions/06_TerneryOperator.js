const age = 10;
const isAdult = age >= 18 ? "Adult" : "not an adult";

console.log(isAdult);

const money = false;

const isMoney = money === true ? "buy products" : "they should bring money";
console.log(isMoney);
